[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "embedkd"
version = "0.1.5"
description = "Reproducibility-first toolkit for knowledge distillation of metric embeddings"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "EmbedKD contributors" }]
keywords = ["knowledge-distillation", "metric-learning", "image-retrieval", "embedding"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "torch>=2.2",
    "timm>=1.0",
    "numpy>=1.24",
    "pyyaml>=6.0",
    "pillow>=9.0",
]

[project.optional-dependencies]
onnx = ["onnx>=1.15", "onnxruntime>=1.17"]
plots = ["matplotlib>=3.7"]
docs = ["mkdocs-material>=9.5"]
dev = [
    "pytest>=8.0",
    "ruff>=0.16.4,<0.17",   # bounded: ruff changes its default rule set between minors
    "onnx>=1.15",
    "onnxruntime>=1.17",
]

[project.scripts]
embedkd = "embedkd.cli:main"

[project.urls]
Homepage = "https://github.com/hublinhdn/embedkd"
Repository = "https://github.com/hublinhdn/embedkd"

[tool.setuptools.packages.find]
include = ["embedkd*"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
# Declared explicitly rather than left to the default set. The default set
# is not a stable contract: ruff 0.16 widened it, which turned a green CI
# into thirty three failures on code that had not changed. What CI enforces
# now moves only when this list moves.
select = ["E4", "E7", "E9", "F"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["slow: end-to-end tests that train a tiny model on CPU"]
